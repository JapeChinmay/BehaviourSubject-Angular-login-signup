import { ToShortenPipe } from './to-shorten.pipe';

describe('ToShortenPipe', () => {
  it('create an instance', () => {
    const pipe = new ToShortenPipe();
    expect(pipe).toBeTruthy();
  });
});
